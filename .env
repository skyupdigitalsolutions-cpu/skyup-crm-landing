VITE_FORM_ENDPOINT=https://skyup-crm-backend.onrender.com/website-webhook

 
# ⬇️ REQUIRED — the CRM never displays an existing secret back to you (see the
# "Edit Website Source" panel: "Leave the Webhook Secret blank to keep your
# existing secret"). Paste the ORIGINAL secret you set when you first created
# the "SkyupCRM" Website integration. Without it, every lead is silently dropped.
VITE_WEBHOOK_SECRET=Skyup@2026
 